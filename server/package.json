{
  "name": "tienda-online-actualizado",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "next-transpile-modules && babel index.js -d dist",
    "start": "npm run build && node dist/index.js"
  }
  ,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mercadopago": "^2.0.5"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "next-transpile-modules": "^10.0.1"
  }
}
